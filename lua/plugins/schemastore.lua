return {
  "b0o/schemastore.nvim",
  -- Schema catalog for jsonls/yamlls. Consumed by the lspconfig servers
  -- table in lsp.lua — gives schema-aware validation for common config
  -- files (package.json, tsconfig, GitHub Actions, etc.).
  lazy = true,
}
